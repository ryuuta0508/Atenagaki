ご提示いただいた要件定義書に基づき、技術選定と詳細なTODOリストをまとめました。

このプロジェクトの肝は、**「日本の封筒特有の縦書き」**と**「郵便番号枠への正確な印字」**を、いかにメンテナンスしやすい形で実装するかです。

---

### 1. 推奨される技術スタック

ローカル環境での動作と、高精度なPDF生成（特にミリ単位の座標指定）を考慮し、以下の構成を推奨します。

*   **言語:** Python 3.10+
    *   理由: PDF生成ライブラリが強力で、Google Sheets APIとの親和性も高いため。
*   **Webフレームワーク:** Flask
    *   理由: 最小構成に最適。設定が簡単で、ローカル実行に向いている。
*   **PDF生成ライブラリ:** ReportLab
    *   理由: 座標（mm単位）を直接指定して描画できるため、封筒印刷に最も適している。日本の縦書きもフォント設定で対応可能。
*   **Google API:** `google-api-python-client`, `google-auth-oauthlib`
*   **フロントエンド:** HTML5 / CSS (Tailwind CSS) / JavaScript (Vanilla JS)
    *   理由: 追加のビルドツール（React等）を使わず、ブラウザ標準機能で完結させ、メンテナンス性を高める。

---

### 2. 詳細TODOリスト

#### 2.1 準備・環境構築
- [ ] **Google Cloud Platform (GCP) 設定**
    - [ ] プロジェクトの作成。
    - [ ] Google Sheets API の有効化。
    - [ ] サービスアカウントの発行、またはOAuth 2.0クライアントIDの作成。
    - [ ] 認証情報（JSONファイル）のダウンロード。
- [ ] **開発環境の構築**
    - [ ] Python仮想環境の作成 (`python -m venv venv`)。
    - [ ] 必要なライブラリのインストール (`flask`, `reportlab`, `google-api-python-client`等)。
    - [ ] 日本語フォント（IPA明朝やNoto Sans JPなど）の用意（PDF埋め込み用）。

#### 2.2 データ連携・取得ロジック (F-1, F-2)
- [ ] **Sheets API 連携モジュールの作成**
    - [ ] 指定したスプレッドシートIDからデータを配列として取得する関数。
    - [ ] シート名や列名（氏名、住所、郵便番号など）を動的にマッピングする処理。
- [ ] **行指定ロジックの実装**
    - [ ] 指定された開始行・終了行のみをスライスして抽出する機能。

#### 2.3 PDF生成エンジンの開発 (F-3, F-4, F-5, N-2, N-3)
- [ ] **座標管理設定ファイルの作成 (config.py / yaml)**
    - [ ] 封筒サイズ（長形3号、角形2号など）ごとの定義（幅・高さ、郵便番号枠の座標）。
    - [ ] 住所、氏名、差出人の印字開始位置（x, y座標）の定義。
- [ ] **描画ロジックの実装**
    - [ ] ミリメートルをReportLabのポイント（1/72インチ）に変換するユーティリティ関数の作成。
    - [ ] **縦書き処理:** 日本語の縦書き（句読点や長音の向き調整）の実装。
    - [ ] **郵便番号処理:** 7桁の数字を1文字ずつ分割し、封筒の枠に合わせた等間隔配置ロジック。
- [ ] **差出人情報切り替えロジック**
    - [ ] フラグによる差出人印字の有無、固定値の埋め込み。

#### 2.4 Webインターフェースの開発 (F-2, F-6)
- [ ] **メイン画面の作成**
    - [ ] スプレッドシートID入力欄。
    - [ ] 印刷範囲（開始行・終了行）入力欄。
    - [ ] 封筒サイズ選択（ドロップダウン）。
    - [ ] 差出人情報の編集・表示ON/OFF切り替え。
- [ ] **実行・ダウンロード処理**
    - [ ] 「PDF生成」ボタン押下時にバックエンドへPOST送信。
    - [ ] 生成完了後、ブラウザでPDFが自動ダウンロードされるレスポンス処理。

#### 2.5 精度調整・テスト (N-2)
- [ ] **実寸テスト印刷**
    - [ ] 実際の封筒をプリンタにセットし、位置ズレの確認。
    - [ ] 座標設定ファイル（config）の微調整。
- [ ] **バリデーション**
    - [ ] スプレッドシートに空行があった場合の例外処理。
    - [ ] 住所が長すぎる場合の自動改行またはフォントサイズ縮小ロジック。

---

### 3. 実装上のアドバイス

*   **「縦書き」の壁:** ReportLab標準では縦書きのサポートが弱いため、フォント指定時に `@フォント名`（縦書き用フォント）を使うか、1文字ずつ座標を計算して配置する手法をとります。後者の方が、文字間隔を細かく制御できるため、封筒印刷には向いています。
*   **メンテナンス性の工夫 (N-3):** 
    ```python
    # 設定例: こうした値を外部に出しておくことで、CSSのように調整可能にする
    ENVELOPE_CONFIG = {
        "NAGA3": {
            "size": (120*mm, 235*mm),
            "zip_code_start": (45*mm, 210*mm),
            "zip_code_gap": 7*mm,
            "address_start": (100*mm, 180*mm),
            ...
        }
    }
    ```
*   **ローカル実行:** `flask run` で起動し、ブラウザで `localhost:5000` にアクセスする形式で十分要件を満たせます。

次に、具体的なコード（プロトタイプ）の作成に進みますか？それとも、特定の機能（例：郵便番号の枠合わせ）についてより詳細に掘り下げますか？